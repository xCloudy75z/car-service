// Overwritten by scripts/build.js at build time with the real version + timestamp.
// This placeholder is what runs during local dev (serving src/ directly).
export const BUILD = { version: "dev", builtAt: null };
